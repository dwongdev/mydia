mutation StartStreamingSession($fileId: ID!, $strategy: StreamingStrategy!, $maxBitrate: Int) {
  startStreamingSession(fileId: $fileId, strategy: $strategy, maxBitrate: $maxBitrate) {
    sessionId
    duration
  }
}
