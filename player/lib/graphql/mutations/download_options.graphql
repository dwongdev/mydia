mutation DownloadOptions($contentType: String!, $id: ID!) {
  downloadOptions(contentType: $contentType, id: $id) {
    resolution
    label
    estimatedSize
    transcodeStatus
    transcodeProgress
    actualSize
  }
}
